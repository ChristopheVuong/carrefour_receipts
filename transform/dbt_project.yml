name: "carrefour"
version: "1.0.0"
config-version: 2

profile: "carrefour"

model-paths: ["models"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  carrefour:
    staging:
      +materialized: view
    marts:
      +materialized: table
